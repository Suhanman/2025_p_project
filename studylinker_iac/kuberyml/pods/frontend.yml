apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-pod
  labels:
    app: frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
      - name: frontend
        image: nugulsuhan/nugul_repo:latest
        ports:
        - containerPort: 8080
        